{
  "name": "algos-one-princeton",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "pretest": "npm run lint",
    "test": "jest --verbose",
    "build": "tsc && typedoc --out docs src",
    "prestart": "npm run build",
    "start": "node ./lib/index.js"
  },
  "jest": {
    "preset": "ts-jest",
    "tsConfig": "tsconfig.json"
  },
  "engines": {
    "node": "10.19.0"
  },
  "main": "lib/index.js",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "@types/node": "^13.9.8",
    "jest": "^25.2.4",
    "ts-jest": "^25.3.0",
    "ts-node": "^8.8.1",
    "tslint": "^5.12.0",
    "typedoc": "^0.17.3",
    "typescript": "^3.8.3"
  },
  "private": true
}
